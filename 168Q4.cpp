#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void codewithlove() {
    int n;
    cin >> n;

    vector<int> v(n);

    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }

    
}

int main() {
    int t;
    cin >> t;

    while (t--) {
        codewithlove();
    }

    return 0;
}
