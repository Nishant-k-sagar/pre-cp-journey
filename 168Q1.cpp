#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void codewithlove() {
    int n;
    cin >> n;

    // vector<int> v(n);

    // for (int i = 0; i < n; i++) {
    //     cin >> v[i];
    // }

    cout<<n+20<<endl;
}

int main() {
    int t=1;
    cin >> t;

    while (t--) {
        codewithlove();
    }

    return 0;
}
